apiVersion: 1

datasources:
  - name: Threat Radar JSON
    type: simpod-json-datasource
    access: proxy
    url: http://json-server:8000
    isDefault: true
    jsonData:
      cacheDurationSeconds: 60
    editable: true
