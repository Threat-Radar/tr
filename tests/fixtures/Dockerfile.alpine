FROM alpine:3.18

# Install some common packages for testing
RUN apk add --no-cache \
    curl \
    wget \
    git \
    python3 \
    py3-pip \
    openssl \
    ca-certificates

# Add metadata
LABEL description="Test Alpine image for Docker analysis"
LABEL test.distro="alpine"
